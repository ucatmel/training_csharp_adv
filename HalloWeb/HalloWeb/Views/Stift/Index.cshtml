@model IEnumerable<HalloWeb.Models.Stift>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Farbe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Staerke)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IstWasserfest)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Farbe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Staerke)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IstWasserfest)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
